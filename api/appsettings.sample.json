{
    "ConnectionStrings": {
      "DefaultConnection": 
      "ConnectionString"
    },
    "Logging": {
      "LogLevel": {
        "Default": "Information",
        "Microsoft.AspNetCore": "Warning"
      }
    },
    "AllowedHosts": "*",
    "JWT": {
      "Issuer": "JWT issueing service",
      "Audience": "JWT Audience (often the same address as the issuing service",
      "SigningKey": "Secret Key used to help hash and salt JWTs. Must be 512 bytes for hashing algorithm"
    }
  }
  